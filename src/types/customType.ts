export type TheadDataDataType = {
  id: string;
  tooltip: string;
  patch: string;
};

export type CatalogDataType = {
  id: string;
  paint: string;
  date: string;
  title: string;
  recipe: string;
  time: string;
};
